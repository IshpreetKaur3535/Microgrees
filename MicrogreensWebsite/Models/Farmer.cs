namespace MicrogreensWebsite.Models
{
    public class Farmer
    {
        //varibale to store the farmer id in the database
        public int FarmerID { get; set; }

        //varibale to store the farmer namein the database
        public string FarmerName { get; set; }

        //varibale to store the farmerEmail in the database
        public string FarmerEmail { get; set; }

        ////varibale to store the farmerAddress in the database
        public string FarmerAddress { get; set; }
    }
}
